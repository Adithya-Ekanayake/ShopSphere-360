module.exports = [{ id: "returns-high-volume", category: "Returns", evaluate(data, t) { const total = data.returns.length; const returned = data.returns.reduce((sum, r) => sum + Number(r.QuantityReturned || 0), 0); const orders = new Set(data.returns.map((r) => r.OrderKey)).size; const rate = orders ? returned / orders * 100 : 0; if (rate < t.RETURNS_HIGH_RATE_PERCENT) return null; return { id: this.id, category: this.category, severity: "warning", title: "Return volume is high", finding: `${returned.toLocaleString()} units were returned across ${orders.toLocaleString()} orders, a ${rate.toFixed(1)}% return-to-order rate.`, implication: "Returns may be creating avoidable service and margin costs.", recommendation: "Review the highest-volume return reasons and prioritize product or fulfillment fixes.", metricValue: rate, comparisonValue: t.RETURNS_HIGH_RATE_PERCENT, generatedAt: new Date().toISOString() }; } }, { id: "returns-top-reason", category: "Returns", evaluate(data) { if (!data.returns.length) return null; const counts = {}; data.returns.forEach((r) => { counts[r.ReturnReason] = (counts[r.ReturnReason] || 0) + Number(r.QuantityReturned || 0); }); const [reason, value] = Object.entries(counts).sort((a, b) => b[1] - a[1])[0]; return { id: this.id, category: this.category, severity: "info", title: "Return reason needs attention", finding: `${reason} accounts for ${Number(value).toLocaleString()} returned units.`, implication: "This reason is the clearest starting point for reducing returns.", recommendation: `Investigate ${reason.toLowerCase()} in product descriptions, quality checks, and fulfillment handling.`, metricValue: Number(value), comparisonValue: 0, generatedAt: new Date().toISOString() }; } }];
